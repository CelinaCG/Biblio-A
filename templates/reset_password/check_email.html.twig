{% extends 'base.html.twig' %}

{% block title %}Email de réinitialisation du mot de passe envoyé{% endblock %}

{% block body %}
    <div class= "container">
        <div class="mt-5 mb-5">
            <p>Un email a été envoyé contenant un lien que vous devez cliquer pour réinitialiser votre mot de passe.</p>

            <p>Ce lien va expirer dans {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.</p>
        </div>
        <p>Si vous ne recevez pas d'email, merci de vérifier dans votre dossier de spams. Sinon, vous pouvez <a href="{{ path('app_forgot_password_request') }}">réessayer</a>.</p>

    </div>
{% endblock %}
