{{ form_start(form) }}
    
    <div class="row my-3 d-flex justify-content-center">
        <div class="col-md-6 text-uppercase font-weight-bold">
        {{ form_row(form.titre) }}
        </div>
        <div class="col-md-6 text-uppercase font-weight-bold">
        {{ form_row(form.auteur) }}
        </div>
        <div class="col-md-6 text-uppercase font-weight-bold">
        {{ form_row(form.editeur) }}
        </div>
        <div class="col-md-6 text-uppercase font-weight-bold">
        {{ form_row(form.secteur) }}
        </div>
    </div>
    <div class="d-flex justify-content-center">
        <button class="btn create_btn">{{ button_label|default('Envoyer') }}<i class="fa fa-send ml-2"></i></button>
    </div>
{{ form_end(form) }}
