{% extends 'base.html.twig' %}

{% block title %}Administration Bibliothèque Atlantide - Modification de l'ouvrage "{{ book.titre }}"{% endblock %}

{% block body %}
    
    <div class="container">
        
        <h1 class="text-center">Modification de l'ouvrage "{{ book.titre }}"</h1>
        
        <div class="row">
            <div class="col-12">
            
                <div class="card bg-light">
                        
                {{ include('book/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

                <hr>

                <div class="alignement">
       
                    <a class="btn show_btn" href="{{ path('book_index') }}"><i class="fa fa-arrow-left mr-3"></i>Retour à la liste</a>
                    {{ include('book/_delete_form.html.twig') }}
                </div>

               
                </div>
            </div>
        </div>
    </div>
    
{% endblock %}
