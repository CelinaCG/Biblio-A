{{ form_start(form) }}

    <div class="row my-3 d-flex justify-content-center">
        <div class="col-md-5 col-sm-12 text-uppercase font-weight-bold">
        {{ form_row(form.isbn) }}
        </div>
        <div class="col-md-5 col-sm-12 text-uppercase font-weight-bold">
        {{ form_row(form.ean) }}
        </div>
        <div class="col-md-5 col-sm-12 text-uppercase font-weight-bold">
        {{ form_row(form.langue) }}
        </div>
        <div class="col-md-5 col-sm-12 text-uppercase font-weight-bold">
        {{ form_row(form.titre) }}
        </div>
        <div class="col-md-5 col-sm-12 text-uppercase font-weight-bold">
        {{ form_row(form.editeur) }}
        </div>
        <div class="col-md-5 col-sm-12 text-uppercase font-weight-bold">
        {{ form_row(form.annee) }}
        </div>
        <div class="col-md-5 col-sm-12 text-uppercase font-weight-bold">
        {{ form_row(form.format) }}
        </div>
        <div class="col-md-5 col-sm-12 text-uppercase font-weight-bold">
        {{ form_row(form.auteur) }}
        </div>
        <div class="col-md-5 col-sm-12 text-uppercase font-weight-bold">
        {{ form_row(form.coauteur) }}
        </div>
        <div class="col-md-5 col-sm-12 text-uppercase font-weight-bold">
        {{ form_row(form.cote) }}
        </div>
        <div class="col-md-5 col-sm-12 text-uppercase font-weight-bold">
        {{ form_row(form.typeDocument) }}
        </div>
        <div class="col-md-5 col-sm-12 text-uppercase font-weight-bold">
        {{ form_row(form.categorie) }}
        </div>
        <div class="col-md-5 col-sm-12 text-uppercase font-weight-bold">
        {{ form_row(form.resume) }}
        </div>
        <div class="col-md-5 col-sm-12 text-uppercase font-weight-bold">
        {{ form_row(form.imageLivre, {'attr': {'class': 'form-control', 'placeholder': "Choisir une image"}} ) }}
        </div>
        
    </div>
    <div class="d-flex justify-content-center">
        <button class="btn create_btn"><i class="fa fa-floppy-o mr-3"></i>{{ button_label|default('Enregistrer') }}</button>
    </div>
{{ form_end(form) }}
