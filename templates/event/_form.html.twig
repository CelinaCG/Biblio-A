{{ form_start(form) }}
    <div class="font-weight-bold">
        {{ form_widget(form) }}
        <div class="d-flex justify-content-center">
            <button class="btn save_btn"><i class="fa fa-floppy-o mr-3"></i>{{ button_label|default('Enregistrer') }}</button>
        </div>
    </div>
{{ form_end(form) }}
