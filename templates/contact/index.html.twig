{% extends 'base.html.twig' %}

{% block title %}Bibliothèque Atlantide - Contact{% endblock %}

{% block body %}

    <div class="jumbotron jumbotron-fluid" style="background-image:url('{{ absolute_url(asset("images/pexels-pixabay-contact-telephone.jpg")) }}')">
        <div class="container">
            <div class="card d-inline-flex card_opacity">
                <div class="card-body">
                    <h1 class="display-4">Contact</h1>
                    
                </div>
            </div>
        </div>
    </div>

    <div class="container">

        {% for message in app.flashes('message') %}
            <div class="alert alert-success" role="alert">
                {{ message }}
            </div>
        {% endfor %}

    
        <h4>Téléphone</h4>

        <p>10 87 06 42 11</p>

        <h4>Courriel</h4>

        <p>N'hésitez pas à nous contacter en remplissant le formulaire suivant. Tous les champs sont obligatoires.</p>

        <p>Nous vous répondrons dans les meilleurs délais.</p>

        <div class="card bg-light">
            <div class="card-body">
                {{ form_start(contactForm) }}
            
                
                    <div class="row my-3 d-flex justify-content-center">
                        <div class="col-md-10 text-uppercase font-weight-bold">
                            {{ form_row(contactForm.nom) }}
                        </div>
                        <div class="col-md-10 text-uppercase font-weight-bold">
                            {{ form_row(contactForm.prenom) }}
                        </div>
                        <div class="col-md-10 text-uppercase font-weight-bold">
                            {{ form_row(contactForm.email) }}
                        </div>
                        <div class="col-md-10 text-uppercase font-weight-bold">
                            {{ form_row(contactForm.sujet) }}
                        </div>
                        <div class="col-md-10 text-uppercase font-weight-bold">
                            {{ form_row(contactForm.message) }}
                        </div>
                    </div>
                    <div class="d-flex justify-content-center">
                        <button class="btn create_btn">{{ button_label|default('Envoyer') }}<i class="fa fa-send ml-3"></i></button>
                    </div> 

                {{ form_end(contactForm) }}

            </div>
        </div>


        <div class="alignement">
            <a class="btn show_btn" href="{{ path('accueil') }}"><i class="fa fa-arrow-left mr-3"></i>Retour à l'accueil</a>
        </div>

    </div>

{% endblock %}
